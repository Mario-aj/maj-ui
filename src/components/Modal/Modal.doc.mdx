import { Story, Canvas, ArgsTable } from '@storybook/addon-docs/blocks';

# Modal

The Modal component inheritely uses [React Modal](https://github.com/reactjs/react-modal) component and follows the WAI-ARIA Modal Pattern.

<Canvas>
  <Story id="components-modal--base" />
</Canvas>

<ArgsTable />

# Usage

> Note: considering that this library has not yet been published in its first version, we decided not to make it available for use yet. As soon as we have the first version published, we will document the use of all components already available.
