import { ArgsTable, Preview } from '@storybook/addon-docs/blocks';
import { Toggle } from '.';

# Toggle

Toggle component.

## Props

<ArgsTable of={Toggle} />

# Preview

<Preview>
  <Toggle active={false} onClick={() => {}} />
  <Toggle active onClick={() => {}} />
</Preview>

# Sizes

<Preview>
  <Toggle active onClick={() => {}} />
  <Toggle active large onClick={() => {}} />
</Preview>

## Usage

> Note: considering that this library has not yet been published, the path of import is fictitious

```jsx
import { Toggle } from '@maj/react-ui';

const Theme = (): JSX.Element => <Toggle active onClick={() => {}} />;
```
